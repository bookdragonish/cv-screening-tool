import type { Request, Response, NextFunction } from "express";

export async function getExistingCourseRecForCandidate(req: Request, res: Response, next: NextFunction){
    // Get existing course rec for candidate
}

export async function createNewCourseRecForCandidate(req: Request, res: Response, next: NextFunction){

    // Ai call
}